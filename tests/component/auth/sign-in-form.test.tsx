import { cleanup, render, screen } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";
vi.mock("server-only", () => ({}));
import { SignInForm } from "@/features/auth/components/sign-in-form";
afterEach(() => cleanup());
describe("sign-in form", () => { it("renders exact fields and approved links", () => { render(<SignInForm returnTo="/" />); expect(screen.getByRole("textbox", { name: "Email Address" }).getAttribute("name")).toBe("email"); const password = document.getElementById("password") as HTMLInputElement; expect(password.getAttribute("autocomplete")).toBe("current-password"); expect(password.required).toBe(true); const passwordLabels = document.querySelectorAll("label[for='password']"); expect(passwordLabels).toHaveLength(1); expect(passwordLabels[0]?.textContent).toContain("Password"); const forgotPassword = screen.getByRole("link", { name: "Forgot Password?" }); expect(forgotPassword.getAttribute("href")).toBe("/forgot-password"); expect(passwordLabels[0]?.contains(forgotPassword)).toBe(false); expect(screen.getByRole("button", { name: "Show password" })).not.toBeNull(); const counterpart = screen.getByRole("link", { name: "Register now" }); expect(counterpart.getAttribute("href")).toBe("/sign-up"); expect(counterpart.parentElement?.className).toContain("hidden"); expect(counterpart.parentElement?.className).toContain("md:block"); expect(screen.queryByRole("checkbox")).toBeNull(); }); });
