# Project Decision Register

## Purpose

This register separates confirmed source facts from provisional observations and unresolved choices. Update it after each controlled live-verification, design, architecture, dependency, or deployment milestone in `TASKS.md`.

A live response is evidence about the public third-party API at one point in time; it is not permission to inspect another user, bypass authorization, or enable an unsafe feature. When evidence is insufficient, keep the affected feature Conditional or Deferred and continue with unrelated milestones.

## Status definitions

| Status | Meaning |
|---|---|
| **Open** | No responsible implementation choice can be made from current evidence. |
| **Provisional** | A planning default exists, but controlled live evidence must confirm it before dependent integration. |
| **Confirmed** | Directly supported by the Postman source, existing repository, or sanitized live evidence. Include the evidence location. |
| **Conditional** | Work may proceed only if a named safety, authorization, or contract gate passes. |
| **Deferred** | Intentionally excluded because evidence or supported capability is absent. |
| **Resolved** | A project/architecture/design choice has been made and recorded; reopen only when its assumptions change. |

## Update rules

- Record the date and sanitized evidence path when a live observation changes a status.
- Never paste tokens, cookies, passwords, reset codes, personal emails/phones/addresses, or raw customer/order payloads into this file.
- Use IDs only as redacted placeholders such as `<product-id>` or `<own-user-id>`.
- A single successful response may confirm its observed shape, but it does not prove undocumented lifetime, authorization, idempotency, range, or error guarantees.
- Do not resolve authorization by requesting another user’s resource or by guessing an admin endpoint. Lack of safe evidence keeps the feature Conditional or Deferred.
- If the live API contradicts the inventory, preserve the inventory as source analysis, add sanitized evidence here, and explicitly decide whether the feature adapts or remains disabled.

## Confirmed source and repository facts

| Fact | Status | Evidence |
|---|---|---|
| The source collection contains 33 requests in nine feature folders. | **Confirmed** | `docs/API_INVENTORY.md`; sanitized collection count |
| The only response-field evidence in the collection is a top-level `token` read by signup/sign-in test scripts. | **Confirmed** | `docs/API_INVENTORY.md` response-evidence section |
| Sixteen source requests carried a manual custom `token` header and no Bearer auth configuration. | **Confirmed** | `docs/API_INVENTORY.md` authentication analysis |
| The collection has no address-update request. | **Confirmed** | Address inventory and PRD exclusion |
| The collection has no logout, refresh, or current-user request. | **Confirmed** | Authentication inventory and PRD exclusion |
| The collection has no order-detail, cancellation, return, refund, tracking, webhook, or payment-status request. | **Confirmed** | Orders inventory and PRD exclusions |
| Admin routes and customer order history are conditional. | **Confirmed** | `docs/PRD.md` A6/A7 and `docs/ROUTES.md` |
| Next.js is 16.2.12 with React 19.2.4, TypeScript strict mode, Tailwind 4, and App Router files under `app/`. | **Confirmed** | `package.json`, `tsconfig.json`, and repository tree |
| Local Next 16 documentation is the required implementation reference. | **Confirmed** | Root `AGENTS.md` |
| Filesystem validation found 27 PNG screenshots and 27 HTML exports with 27 matching base filenames and no unmatched files. | **Confirmed** | Direct repository audit on 2026-07-31; `design/stitch/screenshots` and `design/stitch/export` |
| Screenshots are the UI source of truth and exports are reference-only. | **Resolved** | `docs/UI_SPEC.md` source policy |

## API and product decisions

| ID | Question | Status | Current evidence and present decision | Resolve/update milestone | Blocks or constrains |
|---|---|---|---|---|---|
| `API-001` / PRD A1 | What is the API base URL? | **Confirmed** | On 2026-07-31, all nine catalog GET candidates across F03/F04 returned HTTP `200` JSON responses from `https://ecommerce.routemisr.com/api/v1` without redirects. This confirms the base for those observations, not future availability. Evidence: `docs/api/examples/F03_SUMMARY.md` and `docs/api/examples/products/F04_SUMMARY.md`. | D10 records the environment value; revalidate before T06/T07. | All live integration if the host later fails or redirects unexpectedly. |
| `API-002` / PRD A2 | Are category, subcategory, brand, and product GETs public? | **Confirmed** | All nine inventoried category, subcategory, brand, and product GET candidates returned HTTP `200` without authentication on 2026-07-31. This confirms anonymous access for the observed list/detail requests, not future availability or query behavior. Evidence: the F03 and F04 summaries. | Revalidate during C01 integration and before T06/T07. | Catalog integration must still handle later authorization or availability changes safely. |
| `API-003` / PRD A5 | What do all path IDs represent? | **Confirmed** | F03 verified list-derived category, subcategory, and brand IDs against their detail/scoped endpoints. F04 verified that the first product `_id` from `GET /products` addressed product detail and matched detail `data._id`. F09 confirmed wishlist removal uses product `_id` in path. F11 confirmed address removal uses address `_id`. F12 confirmed that `POST /orders/{id}` and `POST /orders/checkout-session/{id}` use the cart `_id` from `GET /cart`. `GET /orders/user/{id}` uses the user `_id` from JWT claims. The product `id` alias remains a Mongoose virtual and must not replace verified `_id`. Evidence: the F03/F04 summaries, F09/F11 address/wishlist evidence, and `docs/api/examples/orders/`. | Maintain. | Product `id` alias must not replace verified `_id` without evidence. |
| `API-004` / PRD A9 | What are the success/error envelopes, statuses, and field types? | **Provisional** | F03/F04 established catalog list/detail success shapes, and F05 observed the documented empty product-list envelope. F06 observed signup/sign-in success and error envelopes. F07 observed recovery success envelopes. F08 observed updateMe `200` with `{ message, user: { name, email, role } }` and duplicate `400`, and changeMyPassword `200` with `{ message, user: { name, email, role }, token }` and invalid current password `400`. F09 observed `GET /wishlist` `200` with `{ status: "success", count, data: Product[] }`, `POST /wishlist` `200` with `{ status: "success", message, data: string[] }` (string product `_id`s), and `DELETE /wishlist/{id}` `200` with `{ status: "success", message, data: string[] }` (remaining product `_id`s). F12 observed: `POST /orders/{cartId}` returns `201` with `{ status: "success", data: Order }` (standard envelope); `POST /orders/checkout-session/{cartId}` returns `200` with `{ status: "success", session: { url, success_url, cancel_url } }`; `GET /orders/user/{userId}` returns `200` with a **bare top-level array** `Order[]` (no wrapper envelope). Evidence: previous milestone evidence files plus `docs/api/examples/orders/create-cash-order.md`, `docs/api/examples/orders/create-checkout-session.md`, and `docs/api/examples/orders/get-user-orders.md`. | F10/F13, then T00. | Error and protected response adapters and unobserved field guarantees. |
| `API-005` | What pagination envelopes, defaults, and bounds are supported? | **Provisional** | F05 confirmed `limit=2` returned two items with `metadata.limit=2` and 28 pages, while `page=2` returned 16 different items with `currentPage=2`, `limit=40`, and `prevPage=1`. These exact tests support the keys, but bounds, arbitrary sizes, out-of-range pages, and keyword pagination remain unresolved. Evidence: `docs/api/examples/products/F05_QUERY_MATRIX.md`. | C09 may use only conservative verified behavior; reopen for broader controls or T00 contract tests. | Pagination UI must not imply unverified bounds or paginated keyword support. |
| `API-006` | Which product query values and combinations work? | **Provisional** | F05 classified all nine candidates. Supported observations: `limit=2`, `page=2`, `sort=price`, `sort=-price`, numeric `price[gte]`/`price[lte]`/range, one list-derived `brand`, one list-derived `category[in]`, repeated `category[in]` keys, `category+sort`, and `category+brand`. `fields=title,price` was Unsupported. Positive keyword behavior, keyword+page, comma-separated categories, and brand+price range remain Unresolved. Use only the matrix allowlist; do not generalize tested values or encodings. Evidence: `docs/api/examples/products/F05_QUERY_MATRIX.md`. | C09 implements only Supported forms; reopen for excluded controls or T00 contract tests. | Product-list controls and filter serialization. |
| `API-007` | Should GET/DELETE bodies and trailing slashes be normalized? | **Provisional** | All nine F03/F04 catalog GETs succeeded with normalized paths, no body, and no trailing slash. This supports that transport shape for catalog reads; DELETE behavior and tolerance elsewhere remain unverified. Preserve exact endpoint segment spelling such as `forgotPasswords`. Evidence: the F03/F04 summaries. | F05–F12. | Request transport serialization outside the observed endpoints. |
| `API-008` | What error/redaction model should the frontend expose? | **Resolved** | C01 uses one throwing `PublicApiError` convention with only a fixed safe code/message and optional numeric status: `invalid-request`, `not-found`, `unavailable`, `invalid-response`, and `upstream-failure`. Raw bodies, headers, redirect targets, full URLs, environment values, Zod diagnostics, and fetch exception text are discarded. Public reads use the approved local `10_000` ms `AbortSignal.timeout` policy, normalize timeout/abort/network failures to `unavailable`, and do not retry. `cache: "no-store"` preserves the existing architecture policy that public catalog requests remain explicitly uncached until a later freshness decision; it is not a new cache duration or revalidation policy. | Maintain through public catalog consumers; reopen only for a concrete error or freshness requirement. | Error UI must consume safe codes only; public caching remains explicitly uncached until separately decided. |
| `API-009` | Which host may supply renderable public catalog media? | **Confirmed** | The F04A reads of `GET /categories`, `GET /brands`, and `GET /products` observed only absolute HTTPS media values from `ecommerce.routemisr.com` for `category.image`, `brand.image`, `product.imageCover`, `product.images[]`, and nested product category/brand image roles. Approve only HTTPS media from that hostname; malformed, non-HTTPS, or other-host values normalize to `null`. This evidence is scoped to those three reads and does not freshly reverify media behavior across all nine catalog endpoints. Evidence: `docs/api/examples/F04A_MEDIA_HOSTS.md`. | C01 implements the allowlist; T06/T07 revalidate deployment behavior. | Public catalog media normalization and remote-image configuration; protected-domain media remains separately gated. |
| `AUTH-001` / PRD A3 | Do signup/sign-in return a usable top-level token? | **Confirmed** | On 2026-08-11, controlled signup returned `201` and both correct sign-ins returned `200`; all three success responses contained a nonempty top-level string `token`. The final sign-in reacquired a token after the duplicate and invalid-credential observations. This confirms acquisition, not protected-request usability. Evidence: `docs/api/examples/auth/signup.md` and `docs/api/examples/auth/signin.md`. | Revalidate protected usability with the first owned protected read in F09–F11. | A00, A02, A03 and all protected features. |
| `AUTH-002` / PRD A4 | Is the custom `token` header the required transport? | **Confirmed** | F08 verified that sending the custom header `token: <token>` is accepted by protected endpoints `PUT /users/updateMe` and `PUT /users/changeMyPassword` (`200`), while omitting or invalidating the token returns `401`. F09 further confirmed custom `token` header works for `GET /wishlist`, `POST /wishlist`, and `DELETE /wishlist/{id}`. F12 confirmed custom `token` header works for `POST /orders/{cartId}` (`201`), `POST /orders/checkout-session/{cartId}` (`200`), cart operations, and address operations. Note: `GET /orders/user/{userId}` does not require the token but accepts it. Evidence: previous milestone evidence files plus `docs/api/examples/orders/`. | Maintain. | A00 and protected API transport. |
| `AUTH-003` | What signup/password/phone validation rules apply? | **Open** | F06 confirmed that one exact request containing string `name`, `email`, `password`, matching `rePassword`, and `phone` was accepted. Duplicate signup returned `409`, but requiredness, accepted formats, password rules, normalization, and other validation behavior remain unknown. Evidence: `docs/api/examples/auth/signup.md`. | F07–F08. | A01, A02, A07–A09. |
| `AUTH-004` | What proof links reset-code verification to reset password? | **Provisional** | In the controlled valid flow, `POST /auth/verifyResetCode` accepted only `{ resetCode }` and returned only a string `status`; no recovery token/proof was returned. `PUT /auth/resetPassword` then accepted only `{ email, newPassword }` and returned a nonempty string `token`. This establishes the observed request handoff without proving the server-side linkage, lifetime, or universal absence of another proof mechanism. Evidence: `docs/api/examples/auth/verify-reset-code.md` and `docs/api/examples/auth/reset-password.md`. | A06–A07; reopen if another controlled observation contradicts this handoff. | A06/A07 must not invent a client-carried recovery token or reset-code field in the reset request. |
| `AUTH-005` | What are token lifetime, refresh, and expiry semantics? | **Open** | F06 observed a three-segment, 265-character JWT-shaped sign-in token with numeric `iat` and `exp`; the decoded `exp` timestamp was `2026-11-09T17:33:59Z`. Decoding did not verify the signature or server enforcement. No refresh endpoint exists, and rotation, revocation, expiry response behavior, and lifecycle guarantees remain unknown. Evidence: `docs/api/examples/auth/signin.md`. | F08 observation; otherwise remain open. | Session expiry UX and long-lived authentication. |
| `AUTH-006` | Where should the frontend session be stored and transported? | **Resolved** | A00 implements a stateless AES-256-GCM sealed `HttpOnly` cookie named `route-store-session`, containing only a version and the acquired raw token. The 32-byte unpadded base64url `SESSION_ENCRYPTION_KEY` is isolated from public environment parsing. Tokens remain server-confined; they never enter client storage, client JavaScript, serialized props, URLs, logs, or public environment variables. A00 validates each token's own future numeric `exp` without verifying JWT signatures or promoting claims. Evidence: A00 session codec/tests, `docs/ARCHITECTURE.md`, and F06 auth examples. | T06 validates deployment provisioning and operational key handling. | Refresh, rotation, revocation, protected-header usability, and upstream session lifecycle remain unresolved. |
| `AUTH-007` | Where do current-user ID, profile fields, and role come from? | **Provisional** | F06 signup and sign-in responses and F08 updateMe/changeMyPassword responses included an object `user` with string `name`, `email`, and `role`. Phone is not returned in user responses. The JWT-shaped token exposed string claim names `id`, `name`, and `role`, but values were discarded and decoding did not establish claim authority. There is still no current-user endpoint; profile freshness and reconciliation rely on verified mutation responses and session identity handoff. Evidence: `docs/api/examples/auth/signup.md`, `docs/api/examples/auth/signin.md`, `docs/api/examples/auth/update-profile.md`, and `docs/api/examples/auth/change-password.md`. | A08 session boundary upgrade. | A08, O00, ADM00, account initial state. |
| `AUTH-008` | Does profile/password change rotate or invalidate the token? | **Confirmed** | On 2026-08-20, F08 confirmed that `PUT /users/updateMe` does not return a replacement token and the existing token remains valid. `PUT /users/changeMyPassword` returns a replacement `token` string and immediately invalidates the previous token upstream (subsequent calls with the old token return `401`). Evidence: `docs/api/examples/auth/update-profile.md` and `docs/api/examples/auth/change-password.md`. | A08/A09 session reconciliation. | A08 (retains active token) and A09 (must replace session token). |
| `AUTH-009` | What does sign-out mean without a logout endpoint? | **Resolved** | A00's `clearSession()` deletes only the local `route-store-session` cookie. It makes no upstream request and does not claim token revocation or sign-out from other devices. | Maintain through account/navigation milestones. | Upstream revocation remains unavailable because no logout contract exists. |
| `AUTH-010` | What are reset-code format, expiry, resend, and rate limits? | **Open** | F07 obtained and successfully verified one controlled reset code, but did not record its literal value or independently probe format, expiry, resend, reuse, or rate limits. The request field remains a string and the code-only request succeeded. Evidence: `docs/api/examples/auth/verify-reset-code.md`. | A06–A07 if a later approved observation is required. | A06 must avoid universal length/character/timer claims; A05–A07 must not add resend or rate-limit behavior without evidence. |
| `ACCOUNT-001` | Does `PUT /users/updateMe` require all fields or allow partial updates? | **Confirmed** | On 2026-08-20, F08 verified that `PUT /users/updateMe` supports partial atomic updates with single-field payloads (`{ name }`, `{ email }`, or `{ phone }`) as well as full payloads (`200`). A08 will send single-field updates. `user.phone` is not returned in the success response. Duplicate email returns `400`. Evidence: `docs/api/examples/auth/update-profile.md`. | A08 profile update. | A08 profile form and action serialization. |
| `WISH-001` | Does wishlist removal use a product ID or wishlist-entry ID? | **Confirmed** | On 2026-08-20, F09 confirmed that `DELETE /wishlist/{id}` uses the product `_id` directly in the URL path. Wishlist items do not have separate entry IDs. Evidence: `docs/api/examples/wishlist/remove-from-wishlist.md`. | W01/W03. | W03 path ID serialization. |
| `WISH-002` | What happens on duplicate wishlist add and what do mutations return? | **Confirmed** | On 2026-08-20, F09 confirmed that `POST /wishlist` returns `{ status: "success", message, data: string[] }` containing all string product `_id`s in the wishlist. Duplicate adds return `200` with `"Product added successfully to your wishlist"` and the unique product ID array (idempotent). `DELETE /wishlist/{id}` returns `{ status: "success", message, data: string[] }` containing remaining product IDs (or `[]` when empty, and returns `200` idempotently if the item was already absent). `GET /wishlist` returns `{ status: "success", count: number, data: Product[] }` containing fully populated product objects. Evidence: `docs/api/examples/wishlist/get-wishlist.md`, `docs/api/examples/wishlist/add-to-wishlist.md`, and `docs/api/examples/wishlist/remove-from-wishlist.md`. | W01–W03. | W01 schema/adapter, W02/W03 mutation reconciliation. |
| `CART-001` | Does `/cart/{id}` use a product ID or cart-line ID, and what type is `count`? | **Open** | The raw collection labels the placeholder as product ID and encodes count as a string; live semantics are unknown. | F10. | W06/W07. |
| `CART-002` | What are quantity bounds, stock behavior, totals, currency, and empty-cart shape? | **Open** | No response evidence. Server values must be authoritative. | F10. | W04–W08 and checkout summary. |
| `CART-003` | Can product detail select an initial quantity atomically? | **Deferred** | `POST /cart` accepts only `productId`; initial implementation adds one item. Quantity changes occur later in the cart through verified `PUT /cart/{id}` behavior. Never repeat POST requests to emulate quantity. | Reopen only if F10 or another reviewed contract proves an atomic quantity-add workflow without undocumented probing. | Product-detail `QuantityStepper`; W05 sends one add request only. |
| `ADDR-001` | What address fields are required and what shapes/statuses are returned? | **Confirmed** | On 2026-08-21, F11 verified that `GET /addresses` returns `{ status: "success", results: number, data: Address[] }`. `POST /addresses` accepts JSON `{ name, details, phone, city }` (all string fields required) and returns `{ status: "success", message, data: Address[] }`. `GET /addresses/{id}` returns `{ status: "success", data: Address }`. `DELETE /addresses/{id}` uses the address `_id` in path and returns `{ status: "success", message, data: Address[] }` containing remaining addresses. Address items have shape `{ _id: string, name: string, details: string, phone: string, city: string }`. Evidence: `docs/api/examples/addresses/`. | X01–X05. | X01 schema/adapter, X02 detail, X03 form, X04 removal, and X05 checkout address mapping. |
| `ADDR-002` | Can addresses be edited? | **Deferred** | No update endpoint exists. Do not expose edit-in-place or an edit route. | Reopen only if a new reviewed API source is added. | Edit controls shown in Stitch remain non-functional/absent. |
| `CHECKOUT-001` / PRD A8 | What checkout-session field contains the redirect and which destinations are safe? | **Confirmed** | On 2026-08-21, F12 verified that `POST /orders/checkout-session/{cartId}?url={returnBase}` returns `{ status: "success", session: { url, success_url, cancel_url } }`. The `$.session.url` field contains a Stripe Checkout URL (`https://checkout.stripe.com/c/pay/cs_test_...`). The API constructs `success_url` by appending `/allorders` and `cancel_url` by appending `/cart` to the provided `url` query parameter. The `url` parameter is not validated or restricted by the API. The frontend must: (1) validate `$.session.url` starts with `https://checkout.stripe.com/` before redirect; (2) provide `APP_ORIGIN` as the `url` parameter so return paths align with routing; (3) handle the fixed `/allorders` and `/cart` suffixes in its routing. Evidence: `docs/api/examples/orders/create-checkout-session.md`. | X07/X08 implementation. | X07 redirect validation and X08 return-path routing. |
| `CHECKOUT-002` | Is order `{id}` the cart ID, and what are cash-order status, cart lifecycle, and idempotency? | **Confirmed** | On 2026-08-21, F12 verified that `POST /orders/{id}` uses the cart `_id` from `GET /cart` as the path parameter. Success status is `201` with `{ status: "success", data: Order }`. After a cash order is created, the cart is **automatically cleared** by the server (subsequent `GET /cart` returns `numOfCartItems: 0` with empty products array, but the cart object still exists at `200`). `paymentMethodType` is `"cash"`, `isPaid` is `false`, and `isDelivered` is `false` at creation. Idempotency and retry behavior remain unverified. Evidence: `docs/api/examples/orders/create-cash-order.md`. | X06 implementation. | X06 cash order flow and cart state reconciliation. |
| `CHECKOUT-003` | What return URL format and payment-result reconciliation are valid? | **Confirmed** | On 2026-08-21, F12 verified the API appends `/allorders` for success and `/cart` for cancel to whatever base URL is provided in the `url` query parameter. No webhook, payment-status-lookup, or completion-callback endpoint exists. Since the fixed suffixes (`/allorders`, `/cart`) don't match the app's route structure, X08 should handle redirects to these paths neutrally. The return page must not declare payment success based on URL alone; it should refresh cart and conditionally order state. Evidence: `docs/api/examples/orders/create-checkout-session.md`. | X08 implementation. | Online checkout outcome UX and return-path routing. |
| `CHECKOUT-004` | Can a saved address be mapped into checkout? | **Confirmed** | On 2026-08-21, F12 verified that both `POST /orders/{cartId}` and `POST /orders/checkout-session/{cartId}` accept `shippingAddress: { details, phone, city }` — exactly the same fields available in saved addresses minus `name`. Map saved-address `details`, `phone`, and `city` directly; omit `name`. Evidence: `docs/api/examples/orders/create-cash-order.md` and `docs/api/examples/orders/create-checkout-session.md`. | X05 implementation. | X05 address-to-checkout mapping. |
| `ORDER-001` / PRD A6 | Is own-user order history authenticated and ownership-enforced? | **Confirmed (no enforcement)** | On 2026-08-21, F12 verified that `GET /orders/user/{userId}` returns `200` with orders for the requested user ID **both with and without a `token` header**. The endpoint does not enforce authentication or ownership. Cross-user testing was not performed per project safety rules, but the lack of any auth requirement is confirmed. Present decision: the frontend must request **only** the authenticated user's own ID (from JWT claims stored in session), never expose the user ID or endpoint path to the client, and treat this as a server-side read only. The route remains available but must follow the ownership-by-construction principle in O00/O01. Evidence: `docs/api/examples/orders/get-user-orders.md`. | O00 gate evaluation. | O01/O02 must use only server-side session identity; the endpoint path and user IDs must never reach client bundles. |
| `ORDER-002` | Is an order-detail route supportable? | **Deferred** | No detail endpoint exists. Do not invent `/account/orders/[orderId]`; list data may not be promoted into a detail contract without new evidence and route review. | Reopen only with a reviewed source/route change. | Order-detail Stitch screen remains reference-only. |
| `ADMIN-001` / PRD A7 | Are `GET /users` and `GET /orders` authorized admin endpoints, and what proves the role? | **Deferred** | Evaluated under ADM00. Checked F06 and ordinary session claims. Decoded tokens and profile responses assign the user role claim as `"user"`. There is no evidence of administrative credentials, elevate endpoint, or role-based access control for customers. Under safety and privacy boundaries, we do not call or probe admin endpoints. Customer navigation and layout omits these routes entirely. | Completed in ADM00. | ADM01/ADM02 and both admin routes. |
| `ADMIN-002` | Which user/order fields are safe to display and log? | **Deferred** | Admin directory schemas remain unverified because no administrative access is available. | ADM00 complete. | None. |

## Architecture and delivery decisions

| ID | Question | Status | Current evidence and present decision | Resolve/update milestone | Blocks or constrains |
|---|---|---|---|---|---|
| `ARCH-001` | Where are third-party API calls made and where are server/client boundaries? | **Resolved** | Route Server Components call server-only endpoint modules directly for reads. Endpoint modules separate public/pre-auth/protected entry points and pass `unknown` JSON through runtime validation and domain adapters before UI use. UI mutations use thin Server Actions that revalidate input, session, and ownership before protected transport. Client Components remain low interactive boundaries and never receive tokens or raw envelopes. Internal Route Handlers are not used as an application proxy. Evidence: installed Next.js 16 guidance and `docs/ARCHITECTURE.md`. | Maintain during C01/A00 and reopen only for a new approved external HTTP endpoint or incompatible verified API requirement. | Implementation must preserve the documented server/client, DTO, and transport boundaries. |
| `ARCH-002` | What folder and route-group structure will be used? | **Resolved** | Keep the existing root `app/` and `@/*` root alias. Use one root layout plus `(shop)`, `(auth)`, `(account)`, and `(checkout)` route groups; `(admin)` remains documentation-only until authorized. Shared components live under `components`, feature compositions/actions under `features`, server infrastructure under `lib`, domain DTOs under `types`, and tests under `tests`. All 26 `ROUTES.md` entries have one intended location in `docs/ARCHITECTURE.md`. | Maintain during implementation; reopen only with an approved route or repository-structure change. | New files must use the documented ownership and import direction; conditional directories remain absent until their gates pass. |
| `ARCH-003` | Which runtime validation approach will guard third-party responses? | **Resolved** | Use Zod `^4.4.3` in server-only operation-specific schemas. Every third-party response enters as `unknown`, is parsed before a separate domain adapter runs, and never becomes an unchecked wire assertion. The installed strict TypeScript `5.9.3` satisfies Zod 4's TypeScript 5.5+ requirement. Evidence: `docs/DEPENDENCIES.md` and `docs/ARCHITECTURE.md`. | Maintain during C01; reopen only if implementation proves an incompatible verified contract or runtime constraint. | C01 and every endpoint adapter must use the resolved validation boundary; Zod must not enter Client Components. |
| `ARCH-004` | What data-fetching, caching, revalidation, and mutation strategy is appropriate? | **Provisional** | Server Components call source endpoint modules directly; Server Actions own UI mutations. With Cache Components disabled, public catalog calls are explicitly uncached until a feature records a shared-cache/tag freshness policy. Authentication and all user-specific account, wishlist, cart, checkout, and order requests use `no-store`; session verification may be memoized only within one server render request. Evidence: installed Next.js 16 guidance and `docs/ARCHITECTURE.md`. | C01 validates transport behavior; feature milestones may record public freshness policy; T03/T06 revalidate production behavior. | No protected data may enter a shared cache; D07 defines no cache duration. |
| `ARCH-005` | How will forms and client validation be managed? | **Resolved** | Use native HTML/React forms, Server Actions, `useActionState`, and `useFormStatus`; validate submitted `FormData` again on the server with Zod and apply browser constraints only when the API/product rule is verified. No form-management package is approved. Evidence: installed Next.js 16 forms guidance and `docs/DEPENDENCIES.md`. | Maintain through form milestones; reopen only for a concrete form requirement the native approach cannot meet. | Auth/address/account forms must keep client boundaries narrow, must not invent validation rules, and must not expose raw third-party errors. |
| `ARCH-006` | What unit, component, and browser test stack will be used? | **Resolved** | Direct Vite is pinned to `8.0.16`, with Vitest, Testing Library, and Playwright retained for their selected unit, component, and browser responsibilities. Vite is not a source of the remaining development-only audit finding. D09's complete dependency, audit, TypeScript, lint, and build gate passed without changing the selected test stack. Evidence: `docs/DEPENDENCIES.md` and the 2026-08-08 D09 verification. | Maintain during C00 and implementation; reopen only for a concrete verified incompatibility. | Test configuration remains governed by the selected responsibilities and the documented development-only audit disposition. |
| `ARCH-007` | Which additional dependencies are approved? | **Resolved** | The approved direct package set includes exact `next@16.3.0` and `eslint-config-next@16.3.0`, with React versions unchanged. Next declares PostCSS `8.5.23` and Sharp `^0.35.3`; standard `npm update nanoid` refreshed the transitive lockfile resolution to patched `nanoid@3.3.18`. Production audit reports zero vulnerabilities; one development-only `js-yaml` high finding remains explicitly documented. D08A approved exact runtime dependency `server-only@0.0.1` because the architecture and installed Next.js 16.3.0 guidance require the explicit `import "server-only"` guard for server-only roots. D09A installed and verified `server-only@0.0.1` without changing unrelated direct dependencies. No forced fix, transitive override, direct Nano ID dependency, prerelease, downgrade, removal, suppression, or security-risk exception was used. | Maintain during D10 and implementation; reopen only for an approved direct dependency or a new verified security finding. | The approved dependency set and lockfile must remain reproducible; development-only audit findings require explicit disposition. |
| `ARCH-008` | Which environment variables exist and which are public/private? | **Resolved** | D10 approves server-only `ECOMMERCE_API_BASE_URL` and `APP_ORIGIN`; A00 adds isolated lazy validation for server-only `SESSION_ENCRYPTION_KEY`, a canonical 32-byte unpadded base64url value used only by the session codec. The API and origin contracts remain unchanged, and no variable uses `NEXT_PUBLIC_`. Tokens, credentials, and session secrets remain runtime server-controlled data. Evidence: `docs/ENVIRONMENT.md`, `lib/env/server.ts`, A00 environment tests, and D10 validation coverage. | Maintain during implementation; deployment-specific provisioning remains with T06/T07. | Live integration, deployment, and checkout redirect semantics remain separately gated. |
| `ARCH-009` | What deployment target and runtime constraints apply? | **Open** | No target is documented. Decision must account for server-side session handling, environment configuration, redirects, logs, and third-party API reachability. | D07 initially; finalize T06. | Production architecture and online checkout URL. |
| `ARCH-010` | What is the supported browser/accessibility baseline? | **Provisional** | Use the installed Next.js 16 modern-browser baseline (Chrome 111+, Edge 111+, Firefox 111+, and Safari 16.4+) without custom polyfills by default, plus the WCAG AA-oriented keyboard, focus, labeling, contrast, reduced-motion, announcement, and touch requirements in `UI_SPEC.md`. Exact automated/manual release gates remain pending. Evidence: local `supported-browsers.md`, `docs/UI_SPEC.md`, and `docs/ARCHITECTURE.md`. | T02 defines and runs the final accessibility/responsive browser matrix. | D08 package choices and implementation must support this baseline; older browsers are not promised. |
| `ARCH-011` | Must every protected API contract be verified before implementation starts? | **Resolved** | No. Use the documented vertical path: public verification and catalog first, then run F06–F12 immediately before their matching feature verticals. F13 is final consolidation and never a global prerequisite. | Maintain in `TASKS.md`; reopen only if release policy changes. | Milestone ordering and dependencies. |

## Design decisions

| ID | Question | Status | Current evidence and present decision | Resolve/update milestone | Blocks or constrains |
|---|---|---|---|---|---|
| `DESIGN-001` | Which Stitch artifact wins when sources differ? | **Resolved** | Approved screenshots are primary; `DESIGN.md` supplies intent; exported HTML/CSS is reference-only; PRD/routes constrain behavior. | D05/D06; reopen only after new approval. | All UI implementation. |
| `DESIGN-002` | Which media is local versus API-driven? | **Resolved** | F04-verified product/gallery/category/brand media and later-verified cart, wishlist, checkout, and order media remain API data and must not be copied locally. D04 classifies 69 unique Stitch references as 4 static roles, 53 API-driven placements, and 12 deferred/unsupported placements. The four static roles now use authorized project-generated sources and validated local WebPs: desktop/mobile heroes, desktop promotion background, and fulfillment banner. Their checksums, formats, dimensions, provenance, semantics, and responsive browser PASS evidence are recorded in `docs/ASSET_MANIFEST.md`; Stitch URLs remain art direction only. Wordmark, auth atmosphere, mobile promotion decoration, empty-state symbols, and interface icons remain text/CSS/future inline SVG rather than downloaded assets. | D04 complete; reopen only for new design approval, rights/provenance changes, or changed media evidence. Use F09–F12/O00 for remaining response media fields. | C00/C02 may use the four recorded static paths; API-owned and deferred media remain gated separately. |
| `DESIGN-003` | How are screenshot features that conflict with product scope handled? | **Deferred** | Social auth, rewards, editable addresses, saved cards/PayPal, reviews/variants, tracking, invoice/returns, profile extras, initial product-detail quantity, and direct payment-success claims remain visual references only. | Reopen individually only with API/route evidence. | Corresponding controls must be absent or non-functional as specified by `UI_SPEC.md`. |
| `DESIGN-004` | Is the UI implementation specification and artifact pairing available? | **Resolved** | `docs/UI_SPEC.md` exists. A 2026-07-31 filesystem audit confirmed 27 screenshot/export filename pairs. Project designation makes screenshots approved and primary; matching export code proves pairing only, not approval. | D06 validation after material changes. | C00 and all visual milestones. |
| `DESIGN-005` | What route owns the account overview and order-details designs? | **Deferred** | `ROUTES.md` contains neither `/account` overview nor an order-detail route. Both designs remain reference-only. | Reopen only through a deliberate PRD/route revision. | Those screens are not implementation milestones. |
| `DESIGN-006` | Where do Account and Orders navigation items go? | **Resolved** | Account targets `/account/profile` when authenticated and `/sign-in?returnTo=%2Faccount%2Fprofile` when anonymous, with an allowlisted relative decoded value. Orders is omitted until O00 passes and O02 ships `/account/orders`. | A04 implements and tests the policy. | Shared header, sidebar, and bottom navigation. |
| `DESIGN-007` | How does the homepage newsletter region behave without an API? | **Resolved** | Keep a static `NewsletterPromo` with approved styling/copy but no form, email input, or submit action. | C02. | Homepage component mapping and accessibility semantics. |
| `DESIGN-008` | Is signup terms consent required? | **Deferred** | No product/legal requirement or approved Terms and Privacy destinations exist. Omit the checkbox and legal links rather than collecting meaningless consent. | Reopen only when both the requirement and destinations are approved. | A01/A02. |
| `DESIGN-009` | What supplies customer profile avatars? | **Deferred** | No verified avatar field or upload API exists. Require no local avatar; use a generic account icon or initials from a verified name. | Reopen only after verified response/update support exists. | Account shell, profile, addresses, wishlist, and orders presentation. |

## Decision review checkpoints

- **After F03–F05:** update API host, public access, envelopes, pagination, fields, and query decisions, then proceed through D07–D10, D04, and public catalog without waiting for protected verification.
- **Immediately before each protected vertical:** run F06 for A00–A04, F07 for A05–A07, F08 for A08–A09, F09 for W01–W03, F10 for W04–W08, F11 for X01–X04, and F12 for X05–X08/O00–O02.
- **After F06–F12 individually:** update only the matching token, identity, recovery, profile, wishlist, cart, address, checkout, or order decisions and preserve all stop reasons.
- **At F13:** consolidate all evidence and remaining gates; do not use it to delay an unrelated or already-verified feature.
- **At D04:** run the static-asset track after D03/D06, independently of D09/D10. D04 and the D07–D10 outputs must all satisfy their own gates before C00.
- **At O00/ADM00:** keep conditional routes absent unless their narrow evidence gate passes.
- **Before T06/T07:** revalidate provisional live-API observations that can change deployment safety, especially host, session, checkout, and authorization behavior.
